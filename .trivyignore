# Trivy ignore file — documented, risk-accepted suppressions only.
# One vulnerability ID per line. Each entry MUST carry a justification + an
# exit condition (when it can be removed). Keep this list short.
#
# (currently empty — the starlette 0.52.1 suppressions were removed once
# apache-airflow 3.2.2 shipped a FastAPI supporting starlette >= 1.3.1, which
# was their documented exit condition.)
